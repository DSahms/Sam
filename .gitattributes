# Normalize line endings. Rust + TS tooling expect LF; the repo stores LF and
# checks out LF on Windows for these source types. Binary assets are marked.
* text=auto eol=lf

*.png binary
*.ico binary
*.woff2 binary
