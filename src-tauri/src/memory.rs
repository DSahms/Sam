//! `memory` — auditable memory candidates.
//!
//! **Phase 7.** Memory-candidate workflow. Conversation content never becomes
//! durable memory automatically; every candidate is reviewed before it can
//! affect retrieval (directive §26).
