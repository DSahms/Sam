//! `crypto` — cryptographic primitives.
//!
//! **Phase 1.** Provides the building blocks used by `vault`, `backup`, and
//! the source store: CSPRNG, Argon2id key derivation, AES-256-GCM, and key
//! wrapping. See `docs/KEY_MANAGEMENT.md`.
