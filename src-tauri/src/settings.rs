//! `settings` — encrypted application settings.
//!
//! **Phase 1 foundation.** Per-vault encrypted settings (provider routing,
//! inactivity lock, sensitivity defaults) and non-sensitive app-level
//! settings. Provider credentials are encrypted at rest (directive §13).
