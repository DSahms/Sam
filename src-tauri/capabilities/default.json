{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default Sammy capability: minimal permissions for the core window. No network, filesystem, or shell access is granted by default — capabilities are added per-feature as the directive requires explicit consent.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default",
    "core:app:default",
    "dialog:default"
  ]
}
