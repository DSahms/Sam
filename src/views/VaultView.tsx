import { Placeholder } from "@/components/Placeholder";

export function VaultView() {
  return (
    <Placeholder
      phase="Phase 1 — Cryptographic Vault Spine"
      summary="Create, unlock, lock, rename, export, and delete independent encrypted vaults. Not yet wired to the backend."
    />
  );
}
