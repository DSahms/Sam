import { Placeholder } from "@/components/Placeholder";

export function ChatView() {
  return (
    <Placeholder
      phase="Phase 2 — Identity, Chat, and Providers"
      summary="Companion chat with streaming, citations, trust classification, and provider-crossing approval. Requires an unlocked vault."
    />
  );
}
