import { Placeholder } from "@/components/Placeholder";

export function MemoryReviewView() {
  return (
    <Placeholder
      phase="Phase 7 — Auditable Memory"
      summary="Approve, edit, reject, defer, mark temporary, correct, supersede, or delete memory candidates. Chat cannot silently create approved facts."
    />
  );
}
