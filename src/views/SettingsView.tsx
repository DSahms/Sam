import { Placeholder } from "@/components/Placeholder";

export function SettingsView() {
  return (
    <Placeholder
      phase="Phase 1+ — Settings"
      summary="Provider routing mode (default: ask before crossing), inactivity lock, sensitivity defaults, and folder grants."
    />
  );
}
