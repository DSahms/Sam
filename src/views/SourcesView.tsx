import { Placeholder } from "@/components/Placeholder";

export function SourcesView() {
  return (
    <Placeholder
      phase="Phase 4 — Secure Source Ingestion"
      summary="Import, extract, OCR, correct, reindex, cite, export, and delete sources. Originals are encrypted at rest."
    />
  );
}
